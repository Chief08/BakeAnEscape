using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class deletelater : MonoBehaviour
{
    public GameObject VRring;

    // Update is called once per frame
    void Update()
    {
        Debug.Log(VRring.transform.position);
    }
}
